<?xml version="1.0" encoding="UTF-8"?>
<project>
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>ru.naumen.gintonic</groupId>
		<artifactId>gintonic.parent</artifactId>
		<version>1.0.1</version>
	</parent>
	<groupId>ru.naumen.gintonic</groupId>
	<artifactId>gintonic.plugin</artifactId>
	<packaging>eclipse-plugin</packaging>
	<name>GinTonic</name>
	<description>GinTonic is an Eclipse plugin for Gin/Guice DI frameworks.</description>

	<build>
		<sourceDirectory>${basedir}/src/</sourceDirectory>
		<resources>
			<resource>
				<directory>${basedir}/icons</directory>
			</resource>
		</resources>
		<outputDirectory>${basedir}/bin</outputDirectory>
	</build>
</project> 
